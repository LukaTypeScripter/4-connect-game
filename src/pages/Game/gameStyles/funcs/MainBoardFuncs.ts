export const firstColumn = (setPosition:React.Dispatch<React.SetStateAction<string>>,setShowImage:React.Dispatch<React.SetStateAction<boolean>>) => {
    setPosition("a / a / a / a");
    setShowImage(true);
  };
 export const secondColumn = (setPosition:React.Dispatch<React.SetStateAction<string>>,setShowImage:React.Dispatch<React.SetStateAction<boolean>>) => {
    setPosition("");
    setPosition("b / b / b / b");
    setShowImage(true);
  };
  export const ThirdColumn = (setPosition:React.Dispatch<React.SetStateAction<string>>,setShowImage:React.Dispatch<React.SetStateAction<boolean>>) => {
    setPosition("");
    setPosition("c / c / c / c");
    setShowImage(true);
  };
  export const fourthColumn = (setPosition:React.Dispatch<React.SetStateAction<string>>,setShowImage:React.Dispatch<React.SetStateAction<boolean>>) => {
    setPosition("");
    setPosition("d / d / d / d");
    setShowImage(true);
  };
  export const fifthColumn = (setPosition:React.Dispatch<React.SetStateAction<string>>,setShowImage:React.Dispatch<React.SetStateAction<boolean>>) => {
    setPosition("");
    setPosition("e / e / e / e");
    setShowImage(true);
  };
  export const sixthColumn = (setPosition:React.Dispatch<React.SetStateAction<string>>,setShowImage:React.Dispatch<React.SetStateAction<boolean>>) => {
    setPosition("");
    setPosition("f / f / f / f");
    setShowImage(true);
  };
  export const seventhColumn = (setPosition:React.Dispatch<React.SetStateAction<string>>,setShowImage:React.Dispatch<React.SetStateAction<boolean>>) => {
    setPosition("");
    setPosition("g / g / g / g");
    setShowImage(true);
  };